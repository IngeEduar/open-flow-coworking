db:
  url: ${DB_URL}
  username: ${DB_USER}
  password: ${DB_PASS}
  pool:
    max-size: 15
    min-idle: 5
    
jpa:
  show-sql: false
  
notifications:
  url: ${NOTIFICATIONS_HOST}
  extraHeaders:
    - name: "Authorization"
      value: "xxxxxx"
  
security:
  jwt:
    secret: ${JWT_SECRET_KEY:QXL/FVes3851K50Jp+qaq+vAXxi4dgt4+GRlS8qm6Vs=}
    expiration:
      value: ${JWT_EXPIRATION_VALUE:6}
      unit: ${JWT_EXPIRATION_UNIT:HOURS}
